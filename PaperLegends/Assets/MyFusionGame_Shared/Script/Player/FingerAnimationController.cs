using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class FingerAnimationController : MonoBehaviour
{
 
  //  public BallController ball; // Gán viên bi vào đây
    
    public void ShootBall()
    {
        //float power = Joystick.Instance.powerSlider.value;
        //Vector2 spinInput = joystick.Direction; // Lấy hướng từ joystick
        //Debug.Log("Bắn bi với lực: " + power);
      //  GameSessionNetWork_Host.Instance.ReleaseBall(); // Gửi lực bắn đến viên bi
    }
 
}
